# Fixtures

Every fixture in this directory is synthetic. Values, timestamps, account labels, and plans do not describe a real account.

Fixtures must never contain copied CLI output, home-directory paths, email addresses, access tokens, refresh tokens, cookies, authorization headers, or raw provider responses. Add a normalized field to the contract before adding it to a fixture.
